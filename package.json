{
  "name": "simple_next_pwg_markdown",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p $PORT",
    "heroku-postbuild": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "export": "next export",
    "deploy": "npm run build && npm run export"
  },
  "dependencies": {
    "marked": "^0.7.0",
    "next": "11.1.0",
    "react": "16.12.0",
    "react-dom": "16.12.0"
  }
}
